<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.uci_sos">

    <application
        android:allowBackup="true"
        android:hardwareAccelerated="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".Buscar"
            android:parentActivityName=".Reservar" />
        <activity android:name=".Datos" />
        <activity android:name=".ReservaRealizada" />
        <activity android:name=".Registro" />
        <activity android:name=".ConfigPlanta" />
        <activity android:name=".Login" />
        <activity android:name=".Reservar" />
        <activity android:name=".MisCamas" />
        <activity android:name=".Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MiHospital" />

    </application>
</manifest>